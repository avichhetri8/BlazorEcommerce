@if (product != null)
{
    <tr>
        <td>@product.Id</td>
        <td>@product.Name</td>
        <td>@product.Brand</td>
        <td>@product.Price</td>
        <td>@product.Description</td>
    </tr>
}

@code {
    [Parameter]
    public Product product{ get; set; }
}
